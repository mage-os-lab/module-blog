<?php

declare(strict_types=1);

/** @var \MageOS\Blog\Block\Tag\View $block */
/** @var \Magento\Framework\Escaper $escaper */

use MageOS\Blog\ViewModel\Tag\Detail as TagDetail;

/** @var TagDetail $viewModel */
$viewModel = $block->getData('view_model');
$tag = $viewModel->getTag();
?>
<?php if ($tag !== null): ?>
    <section class="mageos-blog-tag">
        <header class="mageos-blog-tag__header">
            <h1 class="mageos-blog-tag__title"><?= $escaper->escapeHtml($viewModel->getTitle()); ?></h1>
        </header>

        <?php $description = $viewModel->getDescription(); ?>
        <?php if ($description !== null && $description !== ''): ?>
            <div class="mageos-blog-tag__description">
                <?= /** @noEscape */ $description; ?>
            </div>
        <?php endif; ?>

        <?php $posts = $viewModel->getPosts(); ?>
        <?php if ($posts === []): ?>
            <p class="mageos-blog-tag__empty">
                <?= $escaper->escapeHtml(__('No posts tagged here yet.')); ?>
            </p>
        <?php else: ?>
            <ul class="mageos-blog-tag__posts">
                <?php foreach ($posts as $post): ?>
                    <li class="mageos-blog-tag__post">
                        <h2 class="mageos-blog-tag__post-title">
                            <a href="<?= $escaper->escapeUrl($viewModel->getPostUrl($post)); ?>">
                                <?= $escaper->escapeHtml($post->getTitle()); ?>
                            </a>
                        </h2>
                        <?php $publishDate = $viewModel->getFormattedPublishDate($post); ?>
                        <?php if ($publishDate !== ''): ?>
                            <time class="mageos-blog-tag__post-date"><?= $escaper->escapeHtml($publishDate); ?></time>
                        <?php endif; ?>
                        <?php $excerpt = (string) $post->getShortContent(); ?>
                        <?php if ($excerpt !== ''): ?>
                            <p class="mageos-blog-tag__post-excerpt"><?= $escaper->escapeHtml($excerpt); ?></p>
                        <?php endif; ?>
                    </li>
                <?php endforeach; ?>
            </ul>
        <?php endif; ?>
    </section>
<?php endif; ?>
